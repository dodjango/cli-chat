# Copy this file to .env and fill in your values

# Required: API key (Azure OpenAI key or OpenAI API key)
OPENAI_API_KEY=your_api_key_here

# Required: Base URL of your endpoint
# Azure example:
#   https://<your-azure-openai>.openai.azure.com/openai/v1/
# Local server example:
#   http://localhost:11434/v1/
# OpenAI example (official):
#   https://api.openai.com/v1/
OPENAI_BASE_URL=https://YOUR-ENDPOINT/openai/v1/

# One of these is required:
# For Azure: use your deployment name
OPENAI_DEPLOYMENT=your_deployment_name

# For non-Azure/OpenAI-compatible: model id, e.g. gpt-4o-mini
# OPENAI_MODEL=gpt-4o-mini

# Optional: OpenAI organization (not used for Azure)
# OPENAI_ORG=

# Required for Agents mode with Azure (LiteLLM)
# Azure API version for your Azure OpenAI resource. Example:
# AZURE_API_VERSION=2024-10-01-preview
# Alternatively, you can set AGENTS_AZURE_API_VERSION with the same value.

# Optional for Agents mode: explicit model id for LiteLLM
# If omitted and using Azure with OPENAI_DEPLOYMENT set, agents mode will infer
#   AGENTS_MODEL=azure/<OPENAI_DEPLOYMENT>
# Otherwise, set it explicitly, e.g.:
# AGENTS_MODEL=azure/your_deployment_name

# Optional: Default system prompt for chat sessions (can be overridden via --system)
# OPENAI_SYSTEM_PROMPT=You are a helpful assistant.

# Optional: Assistant display name for interactive mode
# ASSISTANT_NAME=Computer

# Optional: User display name shown before your input prompt
# USER_NAME=You

# Optional: Disable colored output entirely (recognized by many tools)
# Set to any value to disable colorized chat output
# NO_COLOR=1

# Optional: Chat color toggle (overrides NO_COLOR when set to off)
# Values: off|0|false|no to disable colors; anything else enables
# CHAT_COLOR=on

# Optional: Web search MCP (Tavily)
# Sign up at https://tavily.com to obtain an API key if you want reliable web search.
# TAVILY_API_KEY=your_tavily_api_key

# Optional: Global Weather MCP (OpenWeatherMap)
# Sign up at https://openweathermap.org/api to obtain an API key if you want worldwide weather.
# OPENWEATHER_API_KEY=your_openweather_api_key

# Optional: Theme overrides (Rich styles)
# USER_PREFIX_COLOR=bold cyan
# ASSISTANT_PREFIX_COLOR=bold green
# ASSISTANT_TEXT_COLOR=green
# SYSTEM_PREFIX_COLOR=bold magenta
# META_INFO_COLOR=bold dim
